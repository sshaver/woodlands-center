# Seed Exports

`npm run cms:seed` writes `cwmp-content.ndjson` here from the current fixture data.

Import that file with the Sanity CLI after your project and dataset exist. Keep the generated NDJSON out of source control if it contains unpublished or sensitive content.
